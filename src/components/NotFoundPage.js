import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
    <div>
        404! Link with no fullpage Refresh:
        <Link to="/">GO HOME</Link>
    </div>  
);

export default NotFoundPage;